# https://github.com/emersion/xdg-desktop-portal-wlr/issues/190#issuecomment-1144287165
# https://aur.archlinux.org/cgit/aur.git/tree/nvidia.patch?h=wlroots-nvidia
--- a/types/output/render.c
+++ b/types/output/render.c
@@ -311,20 +311,5 @@
 }
 
 uint32_t wlr_output_preferred_read_format(struct wlr_output *output) {
-	struct wlr_renderer *renderer = output->renderer;
-	assert(renderer != NULL);
-
-	if (!renderer->impl->preferred_read_format || !renderer->impl->read_pixels) {
-		return DRM_FORMAT_INVALID;
-	}
-
-	if (!output_attach_back_buffer(output, &output->pending, NULL)) {
-		return false;
-	}
-
-	uint32_t fmt = renderer->impl->preferred_read_format(renderer);
-
-	output_clear_back_buffer(output);
-
-	return fmt;
+	return DRM_FORMAT_XRGB8888;
 }

--- a/render/gles2/renderer.c	2022-02-03 16:19:54.000000000 -0500
+++ b/render/gles2/renderer.c	2022-08-20 09:29:26.937878381 -0400
@@ -159,7 +159,7 @@
 		assert(wlr_egl_is_current(renderer->egl));
 
 		push_gles2_debug(renderer);
-		glFlush();
+		glFinish();
 		glBindFramebuffer(GL_FRAMEBUFFER, 0);
 		pop_gles2_debug(renderer);
 

